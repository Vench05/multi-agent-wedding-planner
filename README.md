
1. install requirements
    `uv sync`

1. Create .env file
1. Add your api key in the .env. Required api key
    ```
        OPENAI_API_KEY=xxx
        TAVILY_API_KEY=xxx
        LANGSMITH_API_KEY=xxx
        LANGSMITH_TRACING=xxx
        LANGSMITH_PROJECT=xxx
    ```

1. Run the main.py